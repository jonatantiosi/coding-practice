palavra_secreta = 'rosario'
palavra_formatada = ['*','*','*','*','*','*','*']

# teste = 'rosario'
# teste.replace('a','*',1)

print(teste)

letra = input('letra: ')

cont = 0

for i in palavra_secreta:
    if letra == i:
        palavra_formatada.repl
        print(cont)
    cont += 1
    
    
    # list_palavra_formatada = list(palavra_formatada)